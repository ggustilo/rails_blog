<h2>Hello, <%= @writer.username %></h2>
<div class="posts">
	<h4>Posts you have written:</h4>
	<% @posts.each do |post| %>
	<div class="post">
		<h5><a href="/posts/<%=post.id%>"><%= post.title %></a></h5>
	<!-- 	<p>Category: <%= post.category %></p>
		<p><%= post.content %></p>
		<p><%= post.requests_for_responses %></p>
		<p><%= post.additional_resources %></p> -->

		<!-- <div class="post_responses">
			<%=post.responses.each do |response| %>
				<div class="response">
					<h6><%= response.title %></h6>
					<p><%= response.content %></p>
					<p class="response_author"><%=response.writer.username %></p>
					<p class="response_date"><%=response.created_at %></p>
				</div>
			<% end %>
		</div> -->
	</div>
	<%end%>
</div>

<div class="responses">
	<h4>Responses you have written:</h4>
			<%=@responses.each do |response| %>
				<div class="response">
					<h6><a href="/posts/<%=response.post.id%>/responses/<%=response.id%>"><%=response.title %></a></h6>
					<!-- <p><%= response.content %></p>
					<p class="response_to"><a href="/posts/<%=response.post.id%>"><%= response.post.title %></a></p> -->
				</div>
			<% end %>
		</div>

</div>
